<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./components/head', { title: 'My products' }) %>
</head>

<body>
    <%- include('./components/navbar', { activeLink: '/products' }) %>

    <section class="container">
        <h1>My Products</h1>
        <div class="cart-items flex flex-col gap-2">
            <% items.forEach((item) => { %>
                <%- include('./components/productItemCard', { item }) %>
            <% }); %>
        </div>
    </section>
</body>

</html>